# Copy this file to .env and fill with real values for local development.
# Do NOT commit .env with secrets.

# Django
SECRET_KEY=dev-secret-change-me
DEBUG=True

# Database (optional SQLite will be used by default)
# DATABASE_URL=sqlite:///db.sqlite3

# Third-party API keys
OPENWEATHER_API_KEY=

# Email (for production set real SMTP creds; dev will use console backend when DEBUG=True)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# Agent runtime flags
AGENTS_MOCK=true
